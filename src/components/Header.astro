---
interface Props {
    title?: string;
}

const { title } = Astro.props;
---

<header class="header">
    <div class="container header-content">
        {/* Logo/Título con Icono SVG */}
        <a href="/" class="logo" transition:name="logo">
            {/* INICIO: SVG del Damero Diamante */}
            <svg
                    xmlns="http://www.w3.org/2000/svg"
                    viewBox="0 0 100 100"
                    aria-labelledby="dameroIconTitle"
                    role="img"
                    class="logo-icon"
            >
                <title id="dameroIconTitle">Icono El Damero</title>
                {/* Partes Doradas */}
                <path d="M50 0 L75 25 L50 50 L25 25 Z M50 50 L75 75 L50 100 L25 75 Z" fill="var(--damero-gold-primary)"/>
                {/* Partes Carbón */}
                <path d="M25 25 L50 50 L25 75 L0 50 Z M75 25 L100 50 L75 75 L50 50 Z" fill="var(--damero-charcoal)"/>
            </svg>
            {/* FIN: SVG del Damero Diamante */}

            <span class="logo-text">EL DAMERO</span>
        </a>

        {/* Navegación Principal */}
        <nav class="nav-primary">
            <ul class="nav-list">
                <li><a href="/" class="nav-link">Inicio</a></li>
                <li><a href="/glosario" class="nav-link">Glosario</a></li>
            </ul>
        </nav>
    </div>
</header>

<style>
    .header {
        position: sticky;
        top: 0;
        z-index: 1000;
        background: var(--damero-black);
        border-bottom: 1px solid var(--damero-line);
        backdrop-filter: blur(8px);
        -webkit-backdrop-filter: blur(8px);
    }

    .header-content {
        display: flex;
        justify-content: space-between;
        align-items: center;
        height: 80px;
        gap: var(--spacing-lg);
    }

    /* ═══════════════════════════════════════════
       LOGO
       ═══════════════════════════════════════════ */

    .logo {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-family: var(--font-serif-header);
        font-size: 1.3rem;
        font-weight: var(--fw-bold);
        letter-spacing: 0.15em;
        color: var(--damero-gold-primary);
        text-decoration: none;
        transition: color var(--transition-fast);
        white-space: nowrap;
        line-height: 1;
        margin: 0; /* Aseguramos reset */
    }

    .logo-text {
        line-height: 1;
        padding-top: 2px;
    }

    .logo-icon {
        height: 1.25em;
        width: auto;
        flex-shrink: 0;
        margin-bottom: -1px;
    }

    .logo:hover {
        color: var(--damero-gold-accent);
    }

    /* ═══════════════════════════════════════════
       NAVEGACIÓN PRINCIPAL
       ═══════════════════════════════════════════ */

    .nav-primary {
        flex: 1;
        display: flex;
        justify-content: flex-end;
        align-items: center;
        height: 100%;
        margin: 0;
    }

    .nav-list {
        display: flex;
        align-items: center;
        list-style: none;
        gap: 2.5rem;
        margin: 0;
        padding: 0;
        height: 100%;
    }

    .nav-list li {
        display: flex;
        align-items: center;
        height: 100%;
        /* CORRECCIÓN CRÍTICA: Eliminamos cualquier margen global heredado (el culpable del 17px) */
        margin: 0;
        padding: 0;
    }

    .nav-link {
        display: flex;
        align-items: center;
        font-size: 0.95rem;
        letter-spacing: 0.05em;
        color: var(--damero-gray-light);
        text-decoration: none;
        position: relative;
        transition: color var(--transition-fast);
        text-transform: uppercase;
        font-weight: var(--fw-semibold);
        line-height: 1;
        height: 100%;
        margin: 0; /* Reset adicional por seguridad */
    }

    .nav-link::after {
        content: '';
        position: absolute;
        bottom: 28px;
        left: 0;
        width: 0;
        height: 1px;
        background: var(--damero-gold-primary);
        transition: width var(--transition-smooth);
    }

    .nav-link:hover {
        color: var(--damero-gold-primary);
    }

    .nav-link:hover::after {
        width: 100%;
    }

    /* ═══════════════════════════════════════════
       RESPONSIVE
       ═══════════════════════════════════════════ */

    @media (max-width: 768px) {
        .header-content {
            height: auto;
            padding: 1rem 0;
            gap: 1.5rem;
            min-height: 70px;
        }

        .logo {
            font-size: 1rem;
            flex-shrink: 0;
            gap: 0.3rem;
        }

        .logo-icon {
            height: 1.2em;
        }

        .nav-primary {
            flex: 0 1 auto;
        }

        .nav-list {
            gap: 1.2rem;
        }

        .nav-link {
            font-size: 0.8rem;
            letter-spacing: 0.04em;
            height: auto;
        }

        .nav-link::after {
            bottom: -3px;
        }
    }

    @media (max-width: 480px) {
        .header-content {
            height: auto;
            padding: 0.75rem 0;
            gap: 1rem;
            min-height: 60px;
        }

        .logo {
            font-size: 0.9rem;
            letter-spacing: 0.12em;
            flex-shrink: 0;
            gap: 0.25rem;
        }

        .logo-icon {
            height: 1.1em;
        }

        .nav-list {
            gap: 0.8rem;
        }

        .nav-link {
            font-size: 0.7rem;
            letter-spacing: 0.03em;
        }
    }
</style>